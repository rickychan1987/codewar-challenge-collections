// #1: Use if else statement to get back the value of
// function makeNegative(num) {
//   if (num < 0) {
//     return num;
//   } else {
//     return (num = num * -1);
//   }
// }

// #2
// function makeNegative(num) {
//   if (num < 0) {
//     return num;
//   } else {
//     return -num;
//   }
// }

// #3
// function makeNegative(num) {
//   return num < 0 ? num : -num;
// }

// #4
// const makeNegative = (num) => (num < 0 ? num : -num);

// #5
// const makeNegative = (num) => -Math.abs(num);

/*Note! with this test the num variable probably is given 1,
you might be open your console and test it and see the given hidden num variable.
*/
